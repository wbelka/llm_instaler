# LLM Installer Dependencies

# Core dependencies
huggingface_hub>=0.20.0
requests>=2.28.0
pyyaml>=6.0
click>=8.1.0        # for CLI
rich>=13.0.0        # for beautiful output
psutil>=5.9.0       # for system checks
safetensors>=0.4.0  # for analyzing files

# API server dependencies (for universal scripts)
fastapi>=0.109.0
uvicorn>=0.27.0
websockets>=12.0
sse-starlette>=2.0.0
pydantic>=2.5.0
python-multipart>=0.0.6
pillow>=10.0.0
numpy>=1.24.0
aiofiles>=23.0.0

# Additional utilities
python-dotenv>=1.0.0

# Development dependencies (for testing)
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0