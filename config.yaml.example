# LLM Installer Configuration File
# Copy this file to config.yaml and customize as needed

# Tokens and authorization
huggingface_token: "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"  # Can be overridden via HF_TOKEN env variable
# Paths (relative to home directory)
models_dir: "~/LLM/models"
cache_dir: "~/LLM/cache"
logs_dir: "~/LLM/logs"

# Installation settings
default_device: "auto"  # auto, cuda, cpu
max_download_workers: 4
resume_downloads: true

# System requirements
min_disk_space_gb: 50
warn_disk_space_gb: 100

# Logging
log_level: "INFO"  # DEBUG, INFO, WARNING, ERROR
log_rotation: "10MB"